description: "참가자 정보"
content:
  application/json:
    schema:
      type: object
      properties:
        participantName:
          type: string
          example: "Sam"
        password:
          type: string
          example: "1q2w!"
required: true